# 서보와 주행 모터 단독 확인

**실물 시험 결과 갱신:** 사용자가 TRAIN·PSD 서보와 TRAIN 모터의 개별 동작 성공을 보고했다. 양쪽 **15번은 SG90 호환품**으로 다른 서보와 회전각 차이가 있다. 같은 펄스값을 그대로 최종 개폐 위치로 사용하지 말고 보드·채널마다 보정한다. 상세 기록은 [HARDWARE_NOTES.md](HARDWARE_NOTES.md)를 참고한다.

기존 `train_sketch`와 `psd_sketch`는 수정하지 않았다. 가상 문 센서를 사용하는 순서 시험은 그대로 보존하고, 이번에는 실제 출력만 수동으로 확인하는 별도 스케치를 사용한다. 자동 주행이나 문 개폐 순서와는 연결하지 않았다.

**TRAIN과 PSD 모두 각 보드의 칩 물리 4번 버튼 하나로 조작한다. 26번(A3) 버튼 추가나 KW11 조작은 필요 없다.** TRAIN은 이미 한 버튼 시험을 마쳤으므로 이번에는 수정한 `psd_actuator_test`를 PSD에만 다시 업로드한다. 앞서 안내한 PSD의 26번 선택·KW11 실행 방식은 폐기했다.

TRAIN의 15번·16번 서보는 둘 다 사용하는 설계다. 기존 신호선을 유지하고, 기구를 분리한 상태에서 각 서보를 하나씩 선택해 시험한다. 두 서보를 동시에 구동하는 통합 문 동작 시험은 아직 아니다. 서보 시험 중에는 주행 모터 전원을 끈다. 전원·신호선을 바꾸거나 업로드할 때도 구동기 전원을 끈다.

## 올릴 파일

| 대상 보드 | 열어서 업로드할 파일 | 시험 가능한 출력 |
| --- | --- | --- |
| TRAIN | `train_actuator_test/train_actuator_test.ino` | D9 서보, D10 서보, D3 주행 모터 |
| PSD | `psd_actuator_test/psd_actuator_test.ino` | D9 서보, D10 서보 |

Arduino Uno / ATmega328P 16 MHz 기준이다. 각 폴더의 헤더 파일을 함께 유지한다. 기존 LiquidCrystal_I2C 라이브러리를 사용하며 Servo 라이브러리는 추가로 필요하지 않다.

**TRAIN용을 PSD에 올리지 않는다.** 같은 D3이 TRAIN에서는 모터 출력이고 PSD에서는 표시부 출력 차단 핀이다. PSD 시험 코드는 표시부를 비활성화하고 부저를 끈다.

이번 단독 시험은 무선을 사용하지 않는다. 한쪽만 올려도 되며, 다른 보드는 꺼 둬도 된다. 기존 통합 코드가 실행 중인 상대 보드에서 통신 FAULT가 뜨는 것은 이 시험에서는 예상된 현상이다. PSD 버튼으로 TRAIN 모터를 정지시키는 무선 기능은 없다.

## 전원과 기구 준비

현재 사용자는 모터를 별도 5V 전원공급기, 서보를 USB 5V로 공급하고 있다. USB의 허용 전류와 서보 기동 전류는 아직 확인되지 않았다. 문 연결 기구를 분리하고, 두 서보를 각각 선택하여 시험한다. 두 서보의 전원 공급 가능 여부는 전원 용량을 확인해야 한다. 떨림·리셋·과열·이상음이 있으면 즉시 구동기 전원을 끄고 전원과 배선을 확인한다. 축을 손으로 붙잡아 정지시키지 않는다.

더 안정적인 시험을 위해 서보 전원도 충분한 전류 용량의 별도 5V 전원에서 분기할 수 있다. 이때:

- 서보 빨강 전원선은 별도 전원 +에만, 갈색/검정 GND는 별도 전원 −에 연결한다. 실제 서보 케이블 표시도 확인한다.
- 별도 전원 −와 해당 MCU GND를 연결한다. 모터 외부 전원도 같은 방식으로 신호 기준 GND가 필요하다.
- USB의 +5V와 외부 전원 +5V를 서로 연결하지 않는다. MCU를 USB로 공급한다면 외부 전원에서는 구동기만 공급하고 GND만 공통으로 한다.
- 서보 전원 가까이에 470~1000µF 전해 커패시터를 둘 경우 +는 전원 +, −는 GND에 연결한다. 두 서보가 같은 전원을 쓰면 전원 분기점의 470µF 하나를 공유할 수 있다. 커패시터는 부족한 전원 용량을 해결하지 못한다.
- 주행 바퀴는 바닥에서 띄우고 모터 몸체를 고정한다. 문 구동/해제 서보는 우선 기구와 분리한다. 1500µs 첫 명령만으로도 현재 위치에서 크게 움직일 수 있다.
- 손이 닿는 곳에 구동기 전원 차단 스위치를 둔다. 소프트웨어 정지는 전원 차단과 다르다.

TowerPro 정품 SG90 안내는 4.8V와 외부 어댑터 사용을 명시한다. BOM은 SG90 180도 호환품이므로 정품의 전류·각도 특성을 그대로 보장할 수 없다. 사용 중인 호환품의 전압 표기를 우선한다. [TowerPro SG90](https://towerpro.com.tw/product/sg90-7/)

BOM의 모터는 NP01D-288이며, 현재 회로와 사용자가 말한 외부 5V 전원을 기준으로 시험한다. 제조사 정격전류·스톨전류는 이번 확인에서 확보하지 못했으므로 더 높은 전압이나 여러 모터 동시 구동으로 확대하지 않는다.

## 조작 핀

물리핀은 **ATmega328P DIP-28 칩의 번호**다. Arduino 보드 단자의 D 번호와 혼동하지 않는다.

| 기능 | Arduino 이름 | 칩 물리핀 | 연결 |
| --- | --- | --- | --- |
| 문 구동용 서보 신호 | D9 / PB1 | 15번 | 기존 서보 신호선 유지 |
| 기계 해제용 서보 신호 | D10 / PB2 | 16번 | 기존 서보 신호선 유지 |
| TRAIN 선택 및 실행 버튼 | D2 / PD2 | **4번** | 기존 버튼을 거쳐 GND |
| TRAIN 주행 PWM | D3 / PD3 | 5번 | 기존 220Ω을 거쳐 IRLZ44N Gate |
| PSD 선택 및 실행 버튼 | D2 / PD2 | **4번** | 기존 버튼을 거쳐 GND |

**양쪽 모두 물리 4번은 Arduino D2다. Arduino D4(물리 6번 KW11)와 다르다.** 단독 시험에서는 A3·KW11을 읽어 동작을 결정하지 않는다. 기존 서보 신호선을 옮기거나 버튼을 추가하지 않는다.

단독 시험에서만 4번 버튼을 선택/실행 겸용으로 사용한다. 따라서 이 시험 중에는 4번을 눌러 비상정지하는 방식이 아니다. 손을 떼면 출력이 중단되고 별도 구동기 전원 차단 수단은 유지한다. 기존 통합 `psd_sketch`의 4번 비상정지 입력과 26번 화면전환 코드 자체는 수정하지 않았다. 현재 26번 버튼이 없는 상태에서 통합 화면전환까지 작동한다고 간주하지 않는다.

## TRAIN 모터 회로 확인

IRLZ44N은 글씨가 보이는 앞면을 보고 다리를 아래로 두었을 때 왼쪽부터 1 Gate, 2 Drain, 3 Source다. 금속 탭도 Drain이다. 같은 외형의 다른 부품에는 이 핀 순서를 적용하지 않는다. [Infineon IRLZ44N 데이터시트](https://www.infineon.com/assets/row/public/documents/24/49/infineon-irlz44n-datasheet-en.pdf?fileId=5546d462533600a4015355e329b91943)

- 외부 5V + → 모터 +.
- 모터 − → IRLZ44N 2번 Drain.
- IRLZ44N 3번 Source → 외부 전원 − 및 MCU GND.
- TRAIN 칩 5번(D3) → 220Ω → IRLZ44N 1번 Gate.
- Gate → 10kΩ → GND. 이 저항은 리셋·업로드 중 Gate가 뜨지 않게 하므로 유지한다.
- 1N5822를 모터 양단에 병렬 연결: **띠 있는 쪽(Cathode)은 모터 +**, 반대쪽은 모터 −.
- 모터 양단 가까이에 100nF 커패시터를 연결한다.

모터를 MCU 핀에 직접 연결하지 않는다. 이 회로는 한 방향 구동이며 OFF는 제동이 아니라 구동 전류를 끄는 동작이다. 바퀴는 관성으로 더 돌 수 있다. 모터가 정지해 웅웅거리거나 MOSFET이 뜨거워지면 누른 채로 버티지 말고 전원을 끈다.

## TRAIN 버튼 하나로 시험하기

1. 구동기 전원을 끄고 새 `train_actuator_test/train_actuator_test.ino`를 TRAIN에 업로드한다. 칩 4번 버튼을 놓는다.
2. 초기 화면은 `T P16 1500us OFF`다. **P16은 칩 16번 서보**, P15는 칩 15번 서보다.
3. 문 기구와 분리한 서보에 전원을 공급하고 4번 버튼을 **1초 이상 계속 누른다**. 누르기 시작한 뒤 약 1초부터 현재 항목의 출력이 나온다. 손을 떼면 출력이 꺼지고, 계속 눌러도 서보는 출력 시작부터 최대 1초 후 꺼진다. 다시 실행하려면 버튼을 놓고 새로 누른다.
4. 버튼을 **짧게 톡 눌렀다 놓으면** 다음 시험값을 선택한다. 선택만으로는 움직이지 않는다. 원하는 화면에서 다시 길게 눌러 시험한다.
5. P16의 1500, 1400, 1600µs를 확인한 다음, P15의 세 값을 같은 방식으로 확인한다. 두 서보 신호선은 15번·16번에 그대로 유지한다.
6. 주행 모터는 바퀴를 바닥에서 띄우고, 서보 전원을 끈 상태에서 시험한다. MOTOR 80부터 시작하여 필요하면 120, 160, 200을 각각 선택한다. 길게 누른 뒤 **출력은 최대 0.7초만** 나온다. 80에서 안 도는 것만으로 고장이라고 단정하지 않는다.
7. 200에서도 안 돌면 더 높은 값이나 전압으로 올리지 말고 5V 전원, 공통 GND, MOSFET 핀, 다이오드 방향을 확인한다. 떨림·리셋·발열·이상음이 있으면 즉시 구동기 전원을 끈다.

선택 순서는 다음과 같다.

| 순서 | 화면과 시험값 |
| --- | --- |
| 1~3 | P16: 1500 → 1400 → 1600µs |
| 4~6 | P15: 1500 → 1400 → 1600µs |
| 7~10 | MOTOR: 80 → 120 → 160 → 200 |
| 다음 | 처음 P16 1500µs로 돌아감 |

짧게 누르기는 약 0.1~0.3초가 편하다. 코드에서는 40ms 미만의 튐을 걸러 내고, 400ms 미만의 누름을 선택으로 판정한다. 400ms 이상 1초 미만으로 애매하게 눌렀다 놓으면 아무 동작도 하지 않는다. 길게 누른 뒤 놓는 동작은 선택으로 처리하지 않는다. 부팅 때 누른 채여도 출력은 시작되지 않고, 먼저 버튼을 놓아야 한다.

지금은 1400/1500/1600µs 세 지점에서 움직이는지 확인하는 단계다. µs는 제어 명령이지 측정된 각도가 아니다. 실제 문 열림·닫힘 및 연결·해제 위치는 아직 정하지 않았다.

## PSD도 4번 버튼 하나로 시험하기

1. 서보 전원을 끄고 수정한 `psd_actuator_test/psd_actuator_test.ino`를 PSD에만 업로드한다. TRAIN은 꺼 둬도 된다.
2. 칩 4번 버튼을 놓은 상태로 시작한다. LCD 초기 화면은 `P P16 1500us OFF`다. 첫 P는 PSD, P16은 칩 16번 서보다.
3. 서보는 문 기구와 분리하고 기존 15번·16번 신호선은 유지한다. 서보 전원을 공급한 뒤 **4번 버튼을 1초 이상 누르고 있으면 현재 항목이 실행**된다.
4. 손을 떼면 출력이 꺼진다. 계속 눌러도 출력 시작 후 최대 1초면 꺼지고, 놓았다 다시 누르기 전까지 반복하지 않는다.
5. **짧게 눌렀다 놓으면** 다음 항목을 선택한다. 선택만으로는 움직이지 않는다.
6. 순서는 **P16 1500 → 1400 → 1600 → P15 1500 → 1400 → 1600 → 처음**이다. PSD에는 MOTOR 메뉴가 없다.

26번 버튼이나 KW11은 사용하지 않는다. LED·세그먼트는 /OE를 HIGH로 유지해 끄고 부저도 끈다. 단독 서보 시험 중 이 표시부들이 꺼져 있는 것은 정상이다.

## LCD 해석과 시험의 한계

- 양쪽 `P16 1500us` / `P15 1400us`: 칩 물리핀과 선택한 펄스 명령.
- 양쪽 `B0` / `B1`: 4번 버튼을 놓음 / 누름.
- 양쪽 `B1 HOLD ...ms`: 길게 누름을 판정하는 중이다. 1초 전에는 출력이 나오지 않는다.
- 양쪽 `RUN B1 ...ms`: 출력 중이며 ms는 남은 출력 시간이다.
- 양쪽 `RELEASE BUTTON`: 먼저 버튼을 놓아야 다음 시험이 가능하다.
- `MOTOR 120`: 선택한 PWM 값이지 RPM이나 실제 속도 측정값이 아니다.
- `ON` / `OFF`: 제어 신호 출력 여부이지 실제 문 위치나 물리적인 정지 확인값이 아니다.

이번 양쪽 단독 시험은 KW11과 가상 V0/V1을 사용하지 않는다. 기존 통합 스케치는 수정하지 않았으며 KW11이 여전히 가상 문 토글로 동작한다. 통합 스케치로 돌아가면 V는 초기값 0부터 다시 맞춘다.

서보 PWM을 끄는 것은 문을 닫거나 기구를 물리적으로 해제하는 명령이 아니다. 서보 종류에 따라 힘이 남거나 자중으로 움직일 수 있다. 실제 문 센서가 없으므로 문 닫힘·잠금 상태를 확인했다고 볼 수 없다.

## 구현과 검증

Timer1은 50Hz 서보 펄스를 만들고, 약 20ms마다 출력 제한 시간과 버튼 입력을 검사한다. TRAIN 모터만 Timer2의 약 977Hz PWM을 사용한다. 두 타이머는 분리되어 있고, 한 번에 하나의 구동 출력만 켠다. 정상 루프에서도 버튼 해제/정지를 확인한다. 인터럽트까지 멈추는 MCU 고장이나 전원 회로 고장까지 보호하는 안전회로는 아니다. [Microchip Timer1 Fast PWM 설명](https://onlinedocs.microchip.com/oxy/GUID-80B1922D-872B-40C8-A8A5-0CBE009FD908-en-US-3/GUID-E1A4CAD2-7AB8-4704-8CCE-09B1718485A2.html)

- Uno 컴파일 통과: TRAIN 한 버튼 버전 8,248 B / SRAM 595 B, PSD 한 버튼 버전 8,178 B / SRAM 595 B.
- 실제 생성된 HEX를 모의 실행하여 TRAIN 10개, PSD 6개 메뉴의 펄스 폭·주기·단독 출력·시간제한·누름 유지 시 재실행 방지를 확인했다.
- TRAIN: 부팅 중 누름 무시, 짧게 누르기 선택, 길게 누르기 실행, 애매한 중간 길이 무시, 버튼 튐, 손 떼기 즉시 중단, 시간 만료 후 재실행 방지, LCD 응답 정지 및 메인 루프 정지 중 출력 중단을 검사했다.
- PSD: 한 버튼 선택/실행, 서보 6개 항목, 부팅 중 누름 무시, 재실행 방지, 버튼 튐, 손 떼기 중단, LCD 응답 정지, 메인 루프 정지 대응과 /OE HIGH·부저 OFF를 확인했다.
- 기존 통합 스케치와 TRAIN 시험 코드는 그대로다. 사용자가 TRAIN 서보 두 개와 모터 개별 동작 성공을 보고했고, PSD도 USBasp 연결 문제 해결 후 서보 시험 성공을 보고했다. 실제 기구 위치 보정과 통합 운행 시험은 아직 남아 있다.

테스트 스크립트는 `tests/run_actuator_gpio.cjs`이며 `node ... <생성된 HEX> train` 또는 `psd`로 실행한다. AVR8js 경로를 AVR8JS_PATH 환경변수로 지정한다. 각각 `tests/run_train_button_gpio.cjs`, `tests/run_psd_button_gpio.cjs`로 연결된다. 두 시험은 조작은 같지만 모터와 /OE 처리 및 보드 가드가 다르므로 각 보드 폴더의 `actuator_bench.h`를 서로 덮어쓰지 않는다. `lcd_safe.h`는 공통이고 각 폴더의 `ui_button.h`는 이전 버전에서 남은 미사용 파일이다.

시험 후 알려줄 결과는 **D9/D10이 각각 움직이는지, 1400과 1600에서 어느 방향인지, 모터가 처음 돌기 시작한 PWM, 리셋·떨림·발열 여부**다. 이 결과로 실제 개폐 위치와 주행 PWM을 정한다. 그다음 가상 문 입력을 유지한 모형용 통합 시험을 별도로 준비한다.
